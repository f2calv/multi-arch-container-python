name: ci

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - "preview/**"
    paths-ignore:
      - .github/copilot-instructions.md
      - .github/dependabot.yml
      - .github/instructions/**
      - LICENSE
      - README.md
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions: {}

jobs:
  lint:
    uses: f2calv/gha-workflows/.github/workflows/lint.yml@v1
    permissions:
      contents: read

  versioning:
    uses: f2calv/gha-workflows/.github/workflows/gha-release-versioning.yml@v1
    permissions:
      contents: read
    with:
      tag-prefix: ''
      tag-and-release: false

  app:
    # Switch to @v1 after the reusable Python workflow is released.
    uses: f2calv/gha-workflows/.github/workflows/app-build-python.yml@f2calv/2026-09-arg-echo
    permissions:
      contents: read
    needs: versioning
    with:
      version: ${{ needs.versioning.outputs.version }}

  image:
    uses: f2calv/gha-workflows/.github/workflows/container-image-build.yml@v1
    permissions:
      packages: write
      contents: read
    needs: [versioning, app]
    with:
      registry: ghcr.io/${{ github.repository_owner }}
      tag: ${{ needs.versioning.outputs.version }}
      platform: linux/amd64,linux/arm64,linux/arm/v7
      push-image: ${{ github.ref_name == github.event.repository.default_branch }}

  release:
    needs: [lint, versioning, app, image]
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    uses: f2calv/gha-workflows/.github/workflows/gha-release-versioning.yml@v1
    permissions:
      contents: write
    with:
      semVer: ${{ needs.versioning.outputs.version }}
      tag-prefix: ''
      move-major-tag: false
